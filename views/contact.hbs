<main class="{{contactUs}}">
		<h1>{{title}}</h1>
		<p>{{msg}}</p>
		<form>
			<select name="subject">
				{{#each formSubject}}
					<option value="{{this.id}}">{{this.title}}</option>
				{{/each}}
			</select>
		</form>	
</main>