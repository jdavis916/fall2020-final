<main class="{{pageMainClass}}">
		<h1>{{title}}</h1>
		<p>{{msg}}</p>



<!-- /.container -->
 <div class="container bgcolor">
<!-- /.row -->
   <div class="row pad1">
     <div class="col-md-4">


  <label class="colortext">Brand</label>
  <div aria-labelledby="dropdownMenuButton">
     <select class="btn-outline btn dropdown-toggle btn-sm bg-white" name="brands">
          {{#each brands}}
            <option value="{{this}}">{{this}}</option>
          {{/each}}
      </select>
  </div>
</div>
<!-- </div> -->

  <div class="col-md-4"><!-- /.Drop down button -->

      <label class="colortext">Models</label>
      <div aria-labelledby="dropdownMenuButton">

     <select class="btn-outline btn dropdown-toggle btn-sm bg-white" name="brands">
          {{#each models}}
            <option value="{{this}}">{{this}}</option>
          {{/each}}
            </select>

      </div>
    </div>

</div>


<!-- /.row -->
   <div class="row">
     <div class="col-md-4">

      <label class="colortext">mileage</label>
      <div aria-labelledby="dropdownMenuButton">

      <select class="btn-outline btn dropdown-toggle btn-sm bg-white" name="brands">
        {{#each mpg}}
          <option value="{{this._id}}">{{this.title}}</option>
        {{/each}}
      </select>

      </div>
</div>

<div class="col-md-4"><!-- /.Drop down button -->

      <label class="colortext">fuel</label>
      <div aria-labelledby="dropdownMenuButton">

     <select class="btn-outline btn dropdown-toggle btn-sm bg-white" name="brands">
        {{#each fuel}}
          <option value="{{this}}">{{this}}</option>
        {{/each}}
      </select>

      </div>
</div>
</div>

<!-- /.row -->
<div class="row">
  <div class="col-md-4">
    <label class="colortext">doors</label>
    <div aria-labelledby="dropdownMenuButton">
      <select class="btn-outline btn dropdown-toggle btn-sm bg-white" name="brands">
          {{#each door}}
        <option value="{{this}}">{{this}}</option>
          {{/each}}
      </select>
    </div>
  </div>

  <div class="col-md-4"><!-- /.Drop down button -->
    <label class="colortext">seats</label>
    <div aria-labelledby="dropdownMenuButton">
      <select class="btn-outline btn dropdown-toggle btn-sm bg-white" name="seats">
          {{#each seat}}
        <option value="{{this}}">{{this}}</option>
          {{/each}}
      </select>
    </div>

</div>
</div>

     <!-- /.row submit button -->
   <div class="row pad1">
	   <div class="col-md-4">

      <a class="nav-link" href="/carList"><input class="btn mt-n1" type="submit" value="Submit" id='rcorners2'></a>
     </div>
   </div>
</div>
 </div>
</main>